<Page
    x:Class="Tech_Tatva__16.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tech_Tatva__16.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource BackgroundBrush}">

    
    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.SecondaryCommands>
                <AppBarButton x:Name="Fav" Label="favourites"/>
                <AppBarButton x:Name="Res" Label="results"/>
                <AppBarButton x:Name="Dev" Label="developers"/>
                <AppBarButton x:Name="Abt" Label="about"/>
            </CommandBar.SecondaryCommands>
            <AppBarButton Icon="Refresh" Label="refresh"/>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <TextBlock Text="techtatva" FontSize="47" Margin="10,10,0,0" Foreground="{StaticResource AppAccentColorBrush}" FontFamily="Segoe WP SemiLight"/>

        <Pivot Grid.Row="1" Margin="10,9.5,10,0">
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" CharacterSpacing="{StaticResource PivotHeaderItemCharacterSpacing}" FontSize="30" FontFamily="Segoe WP Light" Margin="0, 0, 20, 0"/>
                </DataTemplate>
            </Pivot.HeaderTemplate>

            <PivotItem x:Name="Day1" Header="day 1">
                <ListView x:ConnectionId='1' x:Name="List1" SelectionMode="None" IsItemClickEnabled="True"                          > 
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,10">
                                <Image Source="{Binding Image}" Stretch="Fill" Height="51" Width="51"></Image>
                                <TextBlock Text="{Binding Name}" TextAlignment="Center" VerticalAlignment="Center" Margin="20,0,0,0" FontFamily="Segoe WP Light" FontSize="22" ></TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </PivotItem>
            <PivotItem x:Name="Day2" Header="day 2">

            </PivotItem>
            <PivotItem x:Name="Day3" Header="day 3">

            </PivotItem>
            <PivotItem x:Name="Day4" Header="day 4">

            </PivotItem>
            <PivotItem x:Name="Register" Header="register">

            </PivotItem>
            <PivotItem x:Name="Online" Header="online events">

            </PivotItem>
        </Pivot>
    </Grid>
</Page>

